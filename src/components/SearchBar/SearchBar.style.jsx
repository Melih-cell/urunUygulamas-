import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: '#eee',
        margin: 10,
        padding: 10,
        borderRadius: 12,
    }
})